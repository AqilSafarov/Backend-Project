@model TeamMember
@{
    ViewData["Title"] = "Update";
}

<div class="intro-y flex items-center mt-8">
    <h2 class="text-lg font-medium mr-auto">
        Update Team Member
    </h2>
</div>
<div class="grid grid-cols-12 gap-6 mt-5">
    <div class="intro-y col-span-12 lg:col-span-6">
        <!-- BEGIN: Form Layout -->
        <form method="post" enctype="multipart/form-data">
            <div asp-validation-summary="All" style="color:red; margin-bottom:30px;"></div>
            <div class="intro-y box p-5">
                <input asp-for="Id" hidden />
                <input asp-for="Image" hidden />
                <div>
                    <label asp-for="ImageFile">Image</label>
                    <input asp-for="ImageFile" class="input w-full border mt-2">
                </div>
                <div>
                    <label asp-for="Name">Name</label>
                    <input asp-for="Name" class="input w-full border mt-2" placeholder="Input text">
                </div>
                <div>
                    <label asp-for="Surame">Surname</label>
                    <input asp-for="Surame" class="input w-full border mt-2" placeholder="Input text">
                </div>
                <div class="mt-3">
                    <label asp-for="PositionId">Position</label>
                    <div class="mt-2">
                        <select asp-for="PositionId" asp-items="@(new SelectList(ViewBag.Position, "Id", "Name"))" data-placeholder="Select your favorite actors" class="select2 w-full">
                            <option selected="" disabled>Select Category</option>
                        </select>
                    </div>
                </div>



                @*<div class="mt-3 social-menu-basqasi">
                    <span id="addSocial">+</span>
                    @{
                        int i = 0;
                    }
                    @foreach (var item in Model.SocialToTeamMembers)
                    {
                        <div class="social-item">
                            <label asp-for="SocialToTeamMembers[i].SocialId">Social</label>
                            <div class="mt-2">
                                <select asp-for="SocialToTeamMembers[i].SocialId" asp-items="@(new SelectList(ViewBag.Social, "Id", "Name"))" data-placeholder="Select your favorite actors" class="select2 w-full">
                                    <option selected="" disabled>Select Social</option>
                                </select>
                            </div>
                            <span class="deleteSocial">x</span>
                        </div>
                        <hr style="margin:10px 0px" />
                        i++;
                    }

                </div>*@


                <div class="text-right mt-5">
                    <a asp-area="admin" asp-controller="TeamMember" asp-action="index" class="button w-24 border text-gray-700 mr-1">Cancel</a>
                    <button type="submit" class="button w-24 bg-theme-1 text-white">Update</button>
                </div>
            </div>
        </form>
        <!-- END: Form Layout -->
    </div>
</div>

